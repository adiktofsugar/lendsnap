<%= form_for(@client_account) do |f| %>
  <% if @client_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@client_account.errors.count, "error") %> prohibited this client_account from being saved:</h2>

      <ul>
      <% @client_account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
